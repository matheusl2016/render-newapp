# 🎲 Coin Toss Simulator — Streamlit + Render

A lightweight web app built during Sprint 5 to simulate coin flips using **Python**, **Streamlit**, and **Render**.
The app displays real-time results, computes cumulative averages, and stores experiment history using Streamlit's `session_state`.

---

## 🚀 Features

* Slider to choose number of trials
* Button to start the experiment
* Real-time line chart
* Cumulative mean calculation
* Table with all previous runs
* Automatic deployment on Render

---

## ▶️ Run locally

```
pip install -r requirements.txt
streamlit run app.py
```

---

## ☁️ Render Deployment

**Build:** `pip install --upgrade pip && pip install -r requirements.txt`
**Start:** `streamlit run app.py`

---

## 🛠 Tech Stack

Python • Streamlit • SciPy • Pandas • Render • Git/GitHub
